# Practice

## Fetch and Pull Rules

1. `git fetch` checks GitHub for changes but does not change my local files.
2. `git pull` checks GitHub and brings changes into my local branch.
3. `git fetch` is safer when I only want to check first.
4. `git status` shows whether my branch is up to date with `origin/master`.
5. `git branch -vv` shows branch tracking information.